spring:
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8091/
  application:
    name: lisiService
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/